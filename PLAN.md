# HermesX — 双账户对冲实时模拟交易系统

## 策略规则

两个虚拟账户（各 $1000），A 固定做多、B 固定做空同一交易对，形成双向对冲。

| 规则 | 当前值 |
|------|--------|
| 开仓保证金 | 账户余额 × **3%** |
| 杠杆 | **3x** |
| 止损 | 最新加仓/开仓价亏损 **3%** → 该账户平仓 |
| 双止损重置 | 两个账户都止损后，下小时同时重开 |
| 加仓 | 盈利时，按账户权益 × **3%** 加仓 |
| 减仓 | 亏损时，按账户权益 × **3%** 减仓 |
| 触发阈值 | 盈亏绝对值 > **$0.10** 才操作 |
| 手续费 | 开仓 0.02% + 平仓 0.02% |
| 入场价 | 每小时开盘价 |
| 检查间隔 | 3 秒 |
| 数据源 | Binance 合约 bookTicker（实时价）+ REST API（小时K线） |

## 项目结构

```
HermesX/
├── PLAN.md
├── package.json / tsconfig.json
├── output/                     # 回测CSV导出目录
├── src/
│   ├── index.ts                # 入口：启动行情 + 策略 + Web服务
│   ├── config.ts               # 策略参数
│   ├── types.ts                # 类型定义
│   ├── exchange/
│   │   ├── binance.ts          # Binance 合约行情（WS bookTicker + REST K线）
│   │   └── mock.ts             # 离线模拟行情（开发测试用）
│   ├── account/
│   │   └── Account.ts          # 虚拟账户：开仓/加仓/减仓/平仓/P&L/止损检测
│   ├── strategy/
│   │   └── engine.ts           # 策略引擎：3秒止损检查 + 小时边界处理
│   ├── backtest/
│   │   └── runner.ts           # 回测运行器（REST拉取历史数据 + 逐K线执行策略）
│   └── server/
│       ├── app.ts              # Express + WebSocket 推送 + API
│       └── public/
│           ├── index.html      # 仪表盘页面
│           ├── dashboard.js    # Chart.js + 回测UI逻辑
│           └── style.css       # 深色主题样式
```

## 运行方式

| 命令 | 说明 |
|------|------|
| `npm run dev` | 连接 Binance 实时行情，自动开仓 |
| `npm run mock` | 离线模拟（随机价格，开发测试用） |

启动后打开 `http://localhost:3000` 查看仪表盘。

## 仪表盘功能

### 实时模式
- 📈 **P&L 曲线** — A多/B空/合并净值（保留约 1 小时数据）
- 📊 **持仓量柱状图** — 仅小时边界或止损时记录（最多 48 根）
- 💰 **实时数据面板** — 当前价、权益、浮亏%、距止损距离
- 🚨 **止损记录** — 触发时高亮闪烁
- 📋 **交易日志** — 滚动实时日志

### 回测模式
- ⚙️ **配置面板** — 交易对、天数、止损%、杠杆、保证金比例（滑块）
- 🚀 **一键回测** — 从 Binance REST 拉取历史数据运行策略
- 📊 **结果展示** — 汇总卡片（收益率、最大回撤、止损次数）+ P&L 曲线 + 小时级日志（表头固定）
- ⬇ **导出 CSV** — 浏览器下载 + 自动保存到 `output/` 目录

### 日志列说明

```
 时间      | 时 | 开盘价  | 本时盈亏 | 总收益% | A收益% | B收益% | 操作
6/10 01:00  #12  $61368   -25.0     -1.1%     -9.5%   +7.2%    A平仓
```

操作类型：`开仓` / `A加仓` / `A减仓` / `A平仓` / `B平仓` / `AB平仓` / `🔄双重启`

## 回测逻辑

逐根 1h K 线循环：
1. 用上一根 K 线的 H/L 检测是否触发 3% 止损
2. 每根 K 线开盘时计算上小时盈亏 → 盈利加仓 / 亏损减仓（各按权益 3%）
3. 两个账户都止损后，下小时同时重置开仓
4. 每步记录快照（权益、盈亏、操作、累计收益率）

## API 接口

| 路径 | 方法 | 说明 |
|------|------|------|
| `/config.json` | GET | 当前策略配置 |
| `/api/backtest` | POST | 运行回测，返回结果+CSV文件名 |
| `/api/mode` | GET/POST | 切换实时/回测模式 |
| `/output/<file>` | GET | 下载服务端保存的CSV |
