# HermesX — 数字货币合约对冲交易系统

> 项目书（技术方案版）
>
> 版本：v1.3 ｜ 日期：2026-07-14 ｜ 状态：立项评审中
>
> v1.1 更新：杠杆最高 100x；每单大小可设置；止盈止损仅平掉对应周期仓位；周期固定 1m/3m/5m/15m/1h/4h/1d；交易对可自定义
>
> v1.2 更新：止盈止损平仓数量精确 = 开仓数量 Q；解决交易所仓位合并问题（closePosition=false + quantity=Q）
>
> v1.3 更新：支持模拟盘（Testnet）/ 实盘（Live）双环境，API 配置可切换，环境隔离与安全提示

---

## 一、项目概述

### 1.1 项目背景

HermesX 是一套面向加密货币永续合约市场的**自动化对冲交易系统**。系统按用户配置的周期（如每 4 小时），在合约市场**同时开立一个多单和一个空单**，并为每笔订单设置独立的止盈（TP）与止损（SL）。通过多空双向同时建仓 + 不对称止盈止损比的策略设计，捕捉价格波动带来的突破收益。

### 1.2 核心目标

| 目标 | 说明 |
|------|------|
| 自动化周期交易 | 按配置周期（5m/15m/1h/4h/1d）自动建仓，无需人工干预 |
| 双向同时开仓 | 每周期同时开出多单 + 空单（需交易所支持 Hedge 模式） |
| 每单大小可设置 | 用户自定义每笔订单的数量/金额，杠杆最高 100x |
| 独立止盈止损 | 每个周期开出的仓位独立设置 TP/SL，触发后**仅平掉该周期对应方向的仓位**，不影响其他周期持仓 |
| 交易对可自定义 | 用户可自由输入任意合法合约交易对（如 BTCUSDT、ETHUSDT、SOLUSDT 等） |
| 实时监控 | Web 控制台实时展示行情、持仓、盈亏、订单状态 |
| 风控保护 | 最大持仓数、最大亏损、异常熔断等多层风控 |
| 手动干预 | 支持手动平仓、暂停策略、调整参数 |
| **模拟盘/实盘双环境** | 支持配置 Testnet（模拟盘）或 Live（实盘）API 密钥，开发验证用模拟盘，上线切实盘，环境严格隔离 |

### 1.3 目标交易所

优先接入支持 **Hedge Mode（双向持仓）** 的主流交易所：

- **Binance（币安）**— USDT 永续合约，API 文档完善，流动性最好
- **OKX（欧易）**— 支持双向持仓，API 稳定
- **Bybit** — 合约体验好，API 友好

一期接入 Binance，后续通过统一接口扩展。

#### 1.3.1 模拟盘 / 实盘环境

| 环境 | Binance Futures | 用途 | 资金 |
|------|----------------|------|------|
| **Testnet（模拟盘）** | `testnet.binancefuture.com` | 开发、策略验证、新手体验 | 测试资金（免费领取） |
| **Live（实盘）** | `fapi.binance.com` | 正式交易 | 真实资金 |

- ccxt 通过 `exchange.setSandboxMode(true)` 切换至 Testnet 端点
- 系统根据 ApiConfig 中的 `environment` 字段自动选择端点
- **环境隔离**：Testnet 和 Live 使用不同的 API 密钥，互不通用
- **安全提示**：实盘模式下全站显示红色警告条，策略启动前二次确认

---

## 二、核心交易逻辑

### 2.1 策略原理

```
每个周期 T（1m / 3m / 5m / 15m / 1h / 4h / 1d）：
  1. 获取当前标记价格 P
  2. 开多单：方向=LONG，数量=用户设置的每单大小 Q，开仓价≈P
  3. 开空单：方向=SHORT，数量=Q，开仓价≈P
  4. 为本周期多单设置：止盈价 = P × (1 + TP%)，止损价 = P × (1 - SL%)
  5. 为本周期空单设置：止盈价 = P × (1 - TP%)，止损价 = P × (1 + SL%)
  6. 止盈/止损平仓数量 = Q（与开仓数量完全一致）

后续监控（仅针对本周期仓位，精确到数量 Q）：
  - 价格上涨触及多单止盈价 → 平掉【本周期多单的 Q 数量】，记录盈利
  - 价格下跌触及多单止损价 → 平掉【本周期多单的 Q 数量】，记录亏损
  - 价格下跌触及空单止盈价 → 平掉【本周期空单的 Q 数量】，记录盈利
  - 价格上涨触及空单止损价 → 平掉【本周期空单的 Q 数量】，记录亏损
  - ⚠️ TP/SL 触发后仅平掉对应周期的 Q 数量，不影响其他周期持仓
  - ⚠️ 即使同方向有多个周期的仓位合并，也只平掉本周期的 Q
  - 同一周期的多单和空单独立触发，可能先后平仓，也可能同时平仓

示例：
  23:00 周期开仓：多单 0.001 BTC @ 60000 + 空单 0.001 BTC @ 60000
  → 多单 TP 平仓数量 = 0.001 BTC（不是更多，不是更少）
  → 多单 SL 平仓数量 = 0.001 BTC
  → 空单 TP/SL 同理，各平 0.001 BTC

  23:05 又开了一个周期：多单 0.001 BTC + 空单 0.001 BTC
  → 此时账户多头总仓位 = 0.002 BTC（两个周期合并）
  → 但 23:00 周期的 TP 触发时，只平掉 0.001 BTC，剩余 0.001 BTC 不受影响

净收益 = TP盈利 - SL亏损（当 TP% > SL% 且价格单向突破时为正）
```

> **⚠️ 关键技术点 — 仓位合并问题**
>
> 交易所 Hedge 模式下，同一方向的所有仓位是**合并计算**的。例如开了 5 个周期各 0.001 BTC 多单，账户上是一个 0.005 BTC 的多头仓位，而非 5 个独立仓位。
>
> 因此**不能**使用交易所的 `closePosition=true` 参数（会平掉整个 0.005 BTC），而必须使用 `closePosition=false` + 精确指定 `quantity=Q` 的方式，确保每次 TP/SL 只平掉对应周期的 Q 数量。系统在本地维护每个周期仓位的独立记录，与交易所实际仓位做对账。

### 2.2 关键参数

| 参数 | 说明 | 可选值 / 示例 |
|------|------|---------------|
| `cycleInterval` | 建仓周期（固定选项） | `1m` / `3m` / `5m` / `15m` / `1h` / `4h` / `1d` |
| `symbol` | 交易对（用户自定义输入） | BTCUSDT, ETHUSDT, SOLUSDT, 任意合法合约交易对 |
| `quantity` | 每单大小（用户设置） | 按数量：0.001 BTC ｜ 按金额：10 USDT |
| `quantityType` | 每单大小模式 | `BY_QUANTITY`（按数量）/ `BY_NOTIONAL`（按名义金额） |
| `leverage` | 杠杆倍数（1 ~ 100） | 1x ~ 100x，默认 10x |
| `takeProfitPct` | 止盈比例 | 0.1% ~ 50%，默认 1.5% |
| `stopLossPct` | 止损比例 | 0.1% ~ 50%，默认 1.0% |
| `maxOpenPositions` | 最大同时持仓周期组数 | 默认 5 |
| `marginMode` | 保证金模式 | ISOLATED（逐仓，推荐） |
| `positionMode` | 持仓模式 | HEDGE（双向） |
| `tpslScope` | 止盈止损作用范围 | `PER_CYCLE`（仅平本周期对应方向仓位） |
| `tpslQuantity` | 止盈止损平仓数量 | 默认 = `quantity`（Q），即平掉与开仓等量的仓位 |

> **杠杆说明**：交易所对不同交易对的最大杠杆限制不同（如 BTC 最高 125x，某些小币种最高 20x）。系统允许用户设置 1~100x，但实际生效值取用户设置与交易所允许值的较小值。高杠杆伴随高风险，系统会在杠杆 > 50x 时弹出风险提示。
>
> **止盈止损平仓数量说明**：TP/SL 平仓数量 = 开仓数量 Q。例如 23:00 开仓 0.001 BTC，则止盈/止损触发时精确平掉 0.001 BTC，不会多平也不会少平。交易所同方向仓位是合并的，系统通过精确指定平仓数量确保只平掉对应周期的部分。

### 2.3 仓位生命周期

```
[周期触发] → [检查风控] → [开多Q + 开空Q] → [为本周期仓位设置TP/SL（平仓量=Q）]
                                                ↓
                                      [实时监控价格]
                                          ↓        ↓
                        [TP/SL触发：精确平掉本周期对应方向的Q数量]  [手动平仓]
                                          ↓        ↓
                                      [记录盈亏] → [归档订单]
                                      （其他周期仓位不受影响，即使同方向合并也只平Q）
```

**多空独立平仓示例（同一周期，Q=0.001 BTC）**：

```
周期 T 开仓：多单 0.001 BTC @ 60000 + 空单 0.001 BTC @ 60000，TP=1.5%，SL=1.0%
  → 多单 TP价=60900，SL价=59400，平仓量=0.001 BTC
  → 空单 TP价=59100，SL价=60600，平仓量=0.001 BTC

场景 A：价格快速上涨到 60900
  → 多单触发 TP，平掉 0.001 BTC 多单 ✅（盈利 +0.001×900 = +0.9 USDT）
  → 空单仍在持仓，等待后续触发 SL(60600) 或 TP(59100)

场景 B：价格先涨到 60600 再回落到 59100
  → 空单先触发 SL(60600)，平掉 0.001 BTC 空单 ❌（亏损 0.001×600 = -0.6 USDT）
  → 多单仍在持仓，后续触发 TP(60900) 或 SL(59400)

场景 C（多周期并存）：23:00 和 23:05 各开了一个周期，账户多头总仓位=0.002 BTC
  → 23:00 周期的多单 TP 触发 → 只平掉 0.001 BTC（23:00 的 Q）
  → 剩余 0.001 BTC（23:05 周期的）不受影响，继续监控

关键：每个方向独立判断，独立平仓，平仓量精确=Q，互不影响
```

---

## 三、技术选型

### 3.1 选型总览

| 层级 | 技术 | 选型理由 |
|------|------|----------|
| **后端框架** | NestJS (Node.js 20 + TypeScript) | 模块化架构、依赖注入、内置定时任务、WebSocket；与前端共享类型 |
| **交易所接入** | ccxt + binance futures SDK | 统一 API 抽象，支持 100+ 交易所，社区活跃 |
| **ORM** | Prisma | 类型安全、迁移管理、与 TypeScript 深度集成 |
| **定时任务** | NestJS Schedule (`@Cron`) | 周期建仓、TP/SL 轮询、风控运行时监控 |
| **实时通信** | Socket.io | 行情推送、仓位变化、订单状态实时同步前端 |
| **前端框架** | React 18 + TypeScript | 生态成熟、组件丰富 |
| **前端构建** | Vite | 极速 HMR，生产构建优化 |
| **UI 组件** | Tailwind CSS + shadcn/ui | 原子化 CSS + 可定制组件库 |
| **交易图表** | Lightweight Charts (TradingView) | 专业 K 线图，轻量高性能 |
| **数据图表** | ECharts | 盈亏曲线、统计图表 |
| **状态管理** | Zustand + React Query | 轻量全局状态 + 服务端数据缓存 |
| **数据库** | PostgreSQL | 交易记录、策略配置、用户设置，事务可靠 |
| **缓存** | Redis 7 | 实时行情缓存、会话 |
| **容器化** | Docker + Docker Compose | 一致的开发/生产环境 |
| **反向代理** | Nginx | SSL 终止、静态资源、负载均衡 |
| **进程管理** | PM2 | Node 进程守护、日志、重启 |

### 3.2 为什么选 NestJS 而非 Go/Python？

| 维度 | NestJS (TS) | Go (Gin) | Python (FastAPI) |
|------|-------------|----------|-------------------|
| 异步 I/O | ✅ 原生 async/await | ✅ goroutine | ✅ asyncio |
| 交易所 SDK | ✅ ccxt 官方维护 | ⚠️ 社区移植 | ✅ ccxt 原生 |
| 类型安全 | ✅ 全栈 TS 共享类型 | ✅ 强类型 | ❌ 运行时类型 |
| 定时任务 | ✅ @nestjs/schedule | ⚠️ 需自建 | ✅ APScheduler |
| WebSocket | ✅ 原生支持 | ⚠️ 需库 | ✅ 支持 |
| 开发效率 | ✅ 高 | ⚠️ 中 | ✅ 高 |
| 性能 | ✅ 足够（I/O 密集型） | ✅ 最优 | ⚠️ 一般 |

**结论**：交易系统是 I/O 密集型（行情订阅、API 调用、DB 写入），NestJS 的异步能力完全胜任，同时全栈 TypeScript 共享类型定义大幅提升开发效率。若后续性能成为瓶颈，可将行情处理等热路径用 Go 重写为微服务。

---

## 四、系统架构

### 4.1 架构总览

```
┌─────────────────────────────────────────────────────────┐
│                    用户浏览器 (Web UI)                     │
│         React + Tailwind + Lightweight Charts            │
└────────────────────────┬────────────────────────────────┘
                         │ HTTPS / WSS
┌────────────────────────▼────────────────────────────────┐
│                      Nginx (反向代理)                     │
│            SSL终止 / 静态资源 / WebSocket升级              │
└────────┬───────────────────────────────────┬────────────┘
         │ REST API                          │ WebSocket
┌────────▼───────────────────────────────────▼────────────┐
│                   NestJS 后端服务                          │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌────────────┐ │
│  │ API 模块  │ │ 策略引擎  │ │ 订单服务  │ │ 行情服务    │ │
│  │ (REST)   │ │(Scheduler)│ │ (Order)  │ │ (Market WS)│ │
│  └──────────┘ └────┬─────┘ └────┬─────┘ └──────┬─────┘ │
│  ┌──────────┐ ┌────▼─────┐ ┌────▼─────┐ ┌──────▼─────┐ │
│  │ 仓位管理  │ │ TP/SL    │ │ 风控模块  │ │ 实时推送    │ │
│  │(Position)│ │ Monitor  │ │(Risk)    │ │(Gateway)   │ │
│  └──────────┘ └──────────┘ └──────────┘ └────────────┘ │
└────────┬───────────────────────────────────┬────────────┘
         │                          ┌────────▼────────┐
   ┌─────▼─────┐                   │     Redis       │
   │PostgreSQL │                   │ 行情缓存/队列/会话│
   │交易/配置/日志│                  └────────┬────────┘
   └───────────┘                            │
                              ┌─────────────▼──────────────┐
                              │   交易所 API (Binance)       │
                              │   REST: 下单/查询/账户       │
                              │   WS:  行情/订单/仓位推送     │
                              └────────────────────────────┘
```

### 4.2 核心模块职责

#### 4.2.1 行情服务（MarketDataService）

- 通过 WebSocket 订阅交易所实时行情（markPrice、kline）
- 维护内存中的最新价格表，写入 Redis 供其他模块读取
- 断线自动重连，心跳保活
- 提供 `getPrice(symbol)` 接口

#### 4.2.2 策略引擎（StrategyEngine）

- 基于 `@nestjs/schedule` 的 Cron 定时器触发周期任务
- 每周期执行：检查风控 → 获取价格 → 计算开仓参数 → 调用订单服务开多+空 → 设置 TP/SL
- 支持多策略实例（不同交易对、不同参数并行运行）
- 策略状态机：`IDLE → ARMED → OPENING → MONITORING → CLOSING → DONE`

#### 4.2.3 交易所对接服务（ExchangeService）

- 基于 ccxt 封装，统一交易所 API 调用接口
- **双环境支持**：根据 ApiConfig 的 `environment` 字段自动切换
  - `TESTNET`：调用 `exchange.setSandboxMode(true)`，连接 `testnet.binancefuture.com`
  - `LIVE`：连接 `fapi.binance.com`（生产端点）
- 支持同时维护多个交易所实例（模拟盘 + 实盘密钥各一个实例）
- 统一封装：下单、平仓、查询持仓、查询账户、设置杠杆/保证金模式/持仓模式、订阅行情
- API 密钥从数据库加密读取，按 environment 隔离，不混用
- 限频控制：内置速率限制，避免触发交易所频控

#### 4.2.4 订单服务（OrderService）

- 调用 ExchangeService 执行下单 API（市价/限价）
- 开仓：设置杠杆 → 设置保证金模式 → 设置持仓模式 → 下单
- 止盈止损：通过交易所原生条件单（STOP_MARKET / TAKE_PROFIT_MARKET）或本地监控触发
- 订单状态追踪与数据库记录
- 每笔订单记录所属 `environment`，确保模拟盘和实盘订单不混淆

#### 4.2.5 仓位管理（PositionManager）

- 维护当前所有持仓的实时状态（方向、数量、开仓价、当前价、未实现盈亏）
- 通过 WebSocket 订单/仓位推送更新，或定时轮询
- 提供平仓接口（单笔平仓 / 全部平仓 / 按周期平仓）

#### 4.2.6 止盈止损监控（TpSlMonitor）

**核心原则**：TP/SL 触发后仅平掉对应周期、对应方向的 **Q 数量**仓位，不多不少，不影响其他周期的任何持仓。

**仓位合并问题背景**：交易所 Hedge 模式下，同方向多周期仓位是合并的（5 周期各开 0.001 BTC = 总仓位 0.005 BTC）。TP/SL 必须精确平掉 Q=0.001，而非全部 0.005。

- **方案 A（推荐）**：使用交易所原生 STOP_MARKET / TAKE_PROFIT_MARKET 条件单，**关键参数 `closePosition=false` + `quantity=Q`**，确保只平掉 Q 数量。每个周期的多/空仓位各挂一个 TP 单和一个 SL 单，共 4 个条件单/周期。
  - 多单 TP：`side=SELL, positionSide=LONG, type=TAKE_PROFIT_MARKET, stopPrice=TP价, quantity=Q, reduceOnly=true, closePosition=false`
  - 多单 SL：`side=SELL, positionSide=LONG, type=STOP_MARKET, stopPrice=SL价, quantity=Q, reduceOnly=true, closePosition=false`
  - 空单 TP：`side=BUY, positionSide=SHORT, type=TAKE_PROFIT_MARKET, stopPrice=TP价, quantity=Q, reduceOnly=true, closePosition=false`
  - 空单 SL：`side=BUY, positionSide=SHORT, type=STOP_MARKET, stopPrice=SL价, quantity=Q, reduceOnly=true, closePosition=false`
- **方案 B（备用）**：本地价格监控，行情更新时逐个检查每个 OPEN 仓位是否触及 TP/SL，触发后调用交易所市价平仓接口，指定 `quantity=Q, reduceOnly=true`，精确平掉 Q 数量。
- **仓位对账**：系统本地维护每个周期仓位的独立记录（`Position` 表，含 `cycleId` + `side` + `quantity`），定期与交易所实际仓位做对账，确保本地记录与交易所一致。
- 记录触发原因（TP / SL）、触发价格、实际成交价、平仓数量、已实现盈亏

**关键实现细节**：
- ❌ 不使用 `closePosition=true`（会平掉整个方向的所有合并仓位）
- ✅ 使用 `closePosition=false` + 精确 `quantity=Q`
- Hedge 模式下通过 `positionSide=LONG/SHORT` 指定方向
- `reduceOnly=true` 确保只平仓不开仓
- 同一周期多单和空单的 TP/SL 独立计算、独立触发

#### 4.2.7 风控模块（RiskController）

- **建仓前检查**：最大持仓组数、账户可用保证金、单笔最大金额
- **运行时监控**：账户总亏损阈值（达到则停止开新仓）、连续亏损次数限制
- **异常熔断**：API 错误率过高、行情断连、余额异常变动时自动暂停
- **全局开关**：一键停止所有策略、一键平仓所有持仓

#### 4.2.8 实时推送网关（RealtimeGateway）

- Socket.io 服务端，向前端推送：实时行情、仓位变化、订单成交、策略状态、盈亏更新
- 按频道订阅：`market:{symbol}`、`positions`、`orders`、`strategy:{id}`

---

## 五、数据库设计

### 5.1 ER 模型

```
┌─────────────────┐       ┌──────────────────┐       ┌─────────────────┐
│    Strategy     │       │    Position      │       │     Order       │
├─────────────────┤       ├──────────────────┤       ├─────────────────┤
│ id (PK)         │<──┐   │ id (PK)          │<──┐   │ id (PK)         │
│ name            │   │   │ strategyId (FK)  │   │   │ positionId (FK) │
│ symbol          │   │   │ cycleId          │   └──│ side (LONG/SHORT)│
│ cycleInterval   │   │   │ side (LONG/SHORT)│       │ type (MARKET/...)│
│ quantity        │   └──│ entryPrice       │       │ exchangeOrderId │
│ leverage        │       │ quantity         │       │ status          │
│ takeProfitPct   │       │ takeProfitPrice  │       │ price           │
│ stopLossPct     │       │ stopLossPrice    │       │ filledQty       │
│ maxPositions    │       │ status (OPEN/    │       │ createdAt       │
│ apiConfigId(FK) │       │   TP_HIT/SL_HIT/ │       │ updatedAt       │
│ status          │       │   CLOSED/MANUAL) │       └─────────────────┘
│ isActive        │       │ realizedPnl      │
│ createdAt       │       │ closedAt         │       ┌─────────────────┐
│ updatedAt       │       │ cycleOpenTime    │       │  TradeLog       │
└────────┬────────┘       └──────────────────┘       ├─────────────────┤
         │                                           │ id (PK)         │
         │                                           │ positionId (FK) │
         ▼                                           │ action          │
┌─────────────────┐                                   │ detail (JSON)   │
│  ApiConfig      │                                   │ createdAt       │
├─────────────────┤                                   └─────────────────┘
│ id (PK)         │
│ exchange        │
│ environment     │ ← TESTNET / LIVE
│ apiKey (加密)   │
│ apiSecret(加密) │
│ isActive        │
│ createdAt       │
└─────────────────┘
```

### 5.2 关键表说明

- **Strategy**：策略配置，一个策略对应一个交易对 + 一组参数。绑定 `apiConfigId` 指定使用的 API（模拟盘或实盘）
- **Position**：持仓记录，每个周期开出的多/空各一条，通过 `cycleId` 关联同一周期的多空对
- **Order**：订单记录，一个 Position 可能有多个 Order（开仓单 + 止盈单 + 止损单）
- **TradeLog**：交易日志，记录所有关键操作和事件，用于审计和复盘
- **ApiConfig**：交易所 API 密钥，**必须加密存储**。含 `environment` 字段区分模拟盘/实盘

### 5.3 Prisma Schema 示例

```prisma
model Strategy {
  id             String     @id @default(cuid())
  name           String
  symbol         String     // 用户自定义交易对，如 BTCUSDT、ETHUSDT
  cycleInterval  String     // 固定选项：1m / 3m / 5m / 15m / 1h / 4h / 1d
  quantity       Float      // 每单大小
  quantityType   String     @default("BY_QUANTITY") // BY_QUANTITY / BY_NOTIONAL
  leverage       Int        @default(10)  // 1 ~ 100
  takeProfitPct  Float      @default(1.5)
  stopLossPct    Float      @default(1.0)
  maxPositions   Int        @default(5)   // 最大同时持仓周期组数
  marginMode     String     @default("ISOLATED")
  apiConfigId    String?    // 绑定的 API 配置（决定模拟盘/实盘）
  apiConfig      ApiConfig? @relation(fields: [apiConfigId], references: [id])
  status         String     @default("IDLE") // IDLE/RUNNING/PAUSED/STOPPED
  isActive       Boolean    @default(true)
  positions      Position[]
  createdAt      DateTime   @default(now())
  updatedAt      DateTime   @updatedAt

  @@index([symbol, isActive])
}

model Position {
  id              String     @id @default(cuid())
  strategyId      String
  strategy        Strategy   @relation(fields: [strategyId], references: [id])
  cycleId         String     // 同一周期多空共享
  side            String     // LONG / SHORT
  entryPrice      Float
  quantity        Float
  takeProfitPrice Float?
  stopLossPrice   Float?
  status          String     @default("OPEN") // OPEN/TP_HIT/SL_HIT/CLOSED/MANUAL
  realizedPnl     Float?
  cycleOpenTime   DateTime   @default(now())
  closedAt        DateTime?
  orders          Order[]
  logs            TradeLog[]
  createdAt       DateTime   @default(now())
  updatedAt       DateTime   @updatedAt

  @@index([strategyId, status])
  @@index([cycleId])
}

model Order {
  id              String    @id @default(cuid())
  positionId      String
  position        Position  @relation(fields: [positionId], references: [id])
  side            String    // LONG / SHORT
  type            String    // MARKET / STOP_MARKET / TAKE_PROFIT_MARKET
  status          String    // PENDING / FILLED / CANCELED / REJECTED
  price           Float?
  stopPrice       Float?
  filledQty       Float?
  exchangeOrderId String?
  createdAt       DateTime  @default(now())
  updatedAt       DateTime  @updatedAt
}

model TradeLog {
  id         String   @id @default(cuid())
  positionId String?
  position   Position? @relation(fields: [positionId], references: [id])
  action     String   // STRATEGY_START / ORDER_PLACED / TP_HIT / SL_HIT / ...
  detail     Json?
  createdAt  DateTime @default(now())
}

model ApiConfig {
  id          String     @id @default(cuid())
  exchange    String     // BINANCE / OKX
  environment String     @default("TESTNET") // TESTNET(模拟盘) / LIVE(实盘)
  apiKey      String     // AES 加密存储
  apiSecret   String     // AES 加密存储
  isActive    Boolean    @default(true)
  strategies  Strategy[]
  createdAt   DateTime   @default(now())

  @@unique([exchange, environment]) // 同一交易所同一环境只保留一组密钥
}
```

---

## 六、API 接口设计

### 6.1 REST API

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/api/auth/login` | 登录（JWT） |
| GET | `/api/strategies` | 获取策略列表 |
| POST | `/api/strategies` | 创建策略 |
| PUT | `/api/strategies/:id` | 更新策略参数 |
| PATCH | `/api/strategies/:id/status` | 启动/暂停/停止策略 |
| DELETE | `/api/strategies/:id` | 删除策略 |
| GET | `/api/positions` | 获取持仓列表（支持过滤） |
| POST | `/api/positions/:id/close` | 手动平仓 |
| POST | `/api/positions/close-all` | 一键平仓 |
| GET | `/api/orders` | 订单历史 |
| GET | `/api/trades/stats` | 交易统计（总盈亏、胜率等） |
| GET | `/api/market/:symbol/price` | 获取最新价格 |
| GET | `/api/market/:symbol/klines` | 获取 K 线数据 |
| GET | `/api/market/:symbol/leverage-brackets` | 获取交易对杠杆档位（最大允许杠杆、精度） |
| GET | `/api/market/symbols` | 搜索/获取可用合约交易对列表（用于自定义输入校验） |
| GET | `/api/account/balance` | 账户余额（支持 ?environment=TESTNET/LIVE 查询指定环境） |
| GET | `/api/config/exchange` | 获取已配置的 API 列表（含环境标识，密钥脱敏） |
| POST | `/api/config/exchange` | 配置交易所 API 密钥（含 environment 字段：TESTNET / LIVE） |
| DELETE | `/api/config/exchange/:id` | 删除某组 API 配置 |
| GET | `/api/config/exchange/test` | 测试 API 连通性（验证密钥 + 环境是否匹配） |
| POST | `/api/system/emergency-stop` | 紧急停止（全局） |

### 6.2 WebSocket 事件

| 事件 | 方向 | 说明 |
|------|------|------|
| `market:ticker` | S→C | 实时行情推送 |
| `position:update` | S→C | 仓位状态变化 |
| `order:fill` | S→C | 订单成交通知 |
| `strategy:status` | S→C | 策略状态变化 |
| `pnl:snapshot` | S→C | 盈亏快照（定时） |
| `alert:risk` | S→C | 风控告警 |

---

## 七、前端设计

### 7.1 页面结构

```
┌─────────────────────────────────────────────────┐
│  顶栏：Logo / 账户余额 / 紧急停止按钮 / 用户菜单     │
├────────┬────────────────────────────────────────┤
│        │                                        │
│ 侧栏   │              主内容区                    │
│        │                                        │
│ 仪表盘 │  ┌──────────┐ ┌──────────┐ ┌────────┐ │
│ 策略   │  │ 总盈亏    │ │ 活跃持仓  │ │ 胜率   │ │
│ 持仓   │  └──────────┘ └──────────┘ └────────┘ │
│ 订单   │  ┌──────────────────────────────────┐ │
│ 统计   │  │     K线图 + 持仓标记 (Lightweight) │ │
│ 设置   │  └──────────────────────────────────┘ │
│        │  ┌──────────────────────────────────┐ │
│        │  │     活跃持仓表（实时更新）          │ │
│        │  └──────────────────────────────────┘ │
│        │  ┌──────────────────────────────────┐ │
│        │  │     盈亏曲线 (ECharts)            │ │
│        │  └──────────────────────────────────┘ │
└────────┴────────────────────────────────────────┘
```

### 7.2 核心页面

| 页面 | 功能 |
|------|------|
| **仪表盘** | 总览：账户余额、总盈亏、活跃持仓数、策略运行状态、K线图+持仓标记、盈亏曲线 |
| **策略管理** | 创建/编辑/启停策略，配置：交易对（自定义输入）、周期（5m/15m/1h/4h/1d 下拉选择）、每单大小（按数量/按金额）、杠杆（1-100x 滑块）、TP%、SL%、最大持仓组数、**绑定 API 环境（模拟盘/实盘）** |
| **持仓监控** | 实时持仓表：周期ID、方向（多/空）、开仓价、当前价、未实现盈亏、TP/SL 价格、状态、操作（平仓）。同周期多空成对高亮显示 |
| **订单历史** | 全部订单记录，支持筛选和导出 |
| **统计分析** | 按日/周/月统计盈亏、胜率、最大回撤、盈亏比 |
| **设置** | 交易所 API 配置（模拟盘/实盘分别配置）、风控参数、通知设置。实盘配置需二次确认 |

### 7.3 前端目录结构

```
frontend/
├── src/
│   ├── api/           # API 请求封装
│   ├── components/    # 通用组件
│   │   ├── ui/        # shadcn/ui 组件
│   │   ├── charts/    # 图表组件
│   │   └── trading/   # 交易相关组件
│   ├── pages/         # 页面
│   │   ├── Dashboard.tsx
│   │   ├── Strategies.tsx
│   │   ├── Positions.tsx
│   │   ├── Orders.tsx
│   │   ├── Stats.tsx
│   │   └── Settings.tsx
│   ├── hooks/         # 自定义 Hooks
│   ├── stores/        # Zustand 状态
│   ├── types/         # 类型定义（与后端共享）
│   ├── utils/         # 工具函数
│   ├── App.tsx
│   └── main.tsx
├── package.json
├── vite.config.ts
└── tsconfig.json
```

---

## 八、后端目录结构

```
backend/
├── src/
│   ├── modules/
│   │   ├── auth/            # 认证模块
│   │   ├── strategy/        # 策略管理
│   │   ├── order/           # 订单服务
│   │   ├── position/        # 仓位管理
│   │   ├── market/          # 行情服务
│   │   ├── risk/            # 风控模块
│   │   ├── exchange/        # 交易所对接（ccxt 封装）
│   │   ├── trading/         # 策略引擎 + TP/SL 监控
│   │   └── stats/           # 统计分析
│   ├── common/              # 公共模块（拦截器、过滤器、装饰器）
│   ├── config/              # 配置
│   ├── database/            # Prisma
│   ├── gateway/             # WebSocket 网关
│   ├── app.module.ts
│   └── main.ts
├── prisma/
│   ├── schema.prisma
│   └── migrations/
├── test/
├── package.json
├── tsconfig.json
└── nest-cli.json
```

---

## 九、安全与风控

### 9.1 安全设计

| 层面 | 措施 |
|------|------|
| API 密钥存储 | AES-256-GCM 加密，密钥使用环境变量，不落库 |
| **模拟盘/实盘隔离** | Testnet 和 Live 密钥独立存储（`environment` 字段），策略绑定特定环境，不可跨环境交易 |
| **实盘安全提示** | 实盘模式下全站顶部红色警告条；首次切实盘需二次确认；实盘策略启动弹窗确认 |
| **环境切换保护** | 切换 API 环境时自动暂停所有运行中策略，需手动重启 |
| 传输安全 | 全站 HTTPS + WSS，Nginx SSL 终止 |
| 认证 | JWT Token，短期有效 + Refresh Token |
| 接口防护 | Rate Limiting、CORS 白名单 |
| 输入校验 | class-validator 全局管道校验 |
| 日志审计 | 所有交易操作记录 TradeLog（含 environment 字段），不可删除 |
| IP 限制 | 交易所 API 绑定服务器 IP 白名单 |
| API 连通性测试 | 配置密钥后自动测试连通性，验证环境与密钥匹配 |

### 9.2 风控机制

```
┌─────────────────────────────────────────────┐
│              风控检查链                       │
├─────────────────────────────────────────────┤
│                                             │
│  建仓前 → 检查最大持仓数                      │
│        → 检查账户可用保证金                    │
│        → 检查单笔金额上限                      │
│        → 检查连续亏损次数                      │
│                                             │
│  运行中 → 监控账户总未实现亏损                  │
│        → 监控行情连接状态                      │
│        → 监控 API 错误率                      │
│                                             │
│  触发后 → 停止开新仓                          │
│        → 可选：自动平仓所有持仓                 │
│        → 发送告警通知                         │
│                                             │
└─────────────────────────────────────────────┘
```

### 9.3 异常处理

- 交易所 API 限频 → 指数退避重试
- WebSocket 断连 → 自动重连 + 重连后状态同步
- 订单提交失败 → 记录日志 + 通知前端 + 不重试（防止重复下单）
- 服务重启 → 从数据库恢复策略状态，同步交易所实际持仓

---

## 十、部署方案

### 10.1 Docker Compose 编排

```yaml
version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: hermesx
      POSTGRES_USER: hermesx
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: always

  redis:
    image: redis:7-alpine
    command: redis-server --requirepass ${REDIS_PASSWORD}
    volumes:
      - redis_data:/data
    restart: always

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql://hermesx:${DB_PASSWORD}@postgres:5432/hermesx
      REDIS_URL: redis://:${REDIS_PASSWORD}@redis:6379
      JWT_SECRET: ${JWT_SECRET}
      ENCRYPTION_KEY: ${ENCRYPTION_KEY}
      BINANCE_API_KEY: ${BINANCE_API_KEY}
      BINANCE_API_SECRET: ${BINANCE_API_SECRET}
      # 模拟盘密钥（开发/测试用）
      BINANCE_TESTNET_API_KEY: ${BINANCE_TESTNET_API_KEY}
      BINANCE_TESTNET_API_SECRET: ${BINANCE_TESTNET_API_SECRET}
      # 默认环境（TESTNET / LIVE），首次启动用模拟盘
      DEFAULT_ENVIRONMENT: TESTNET
    depends_on:
      - postgres
      - redis
    restart: always

  frontend:
    build: ./frontend
    restart: always

  nginx:
    image: nginx:alpine
    ports:
      - "443:443"
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./nginx/ssl:/etc/nginx/ssl
    depends_on:
      - backend
      - frontend
    restart: always

volumes:
  postgres_data:
  redis_data:
```

### 10.2 部署架构

```
[云服务器 / VPS]
  ├── Nginx (443/80) ─── SSL证书
  ├── Backend (3001) ─── NestJS / PM2
  ├── Frontend (3000) ─── 静态文件由 Nginx 托管
  ├── PostgreSQL (5432)
  └── Redis (6379)
```

**推荐配置**：2核 4G 起步（单用户），带宽 5Mbps+（行情订阅需要稳定低延迟网络）。

**地域选择**：服务器需部署在能访问交易所 API 的地区。

---

## 十一、开发计划与里程碑

### 阶段一：基础框架（Week 1-2）

- [ ] 项目脚手架搭建（NestJS + React + Prisma + Docker）
- [ ] 数据库 Schema 设计与迁移
- [ ] 交易所 SDK 封装（ccxt + Binance Futures，**含 Testnet/Live 双环境切换**）
- [ ] API 配置模块（模拟盘/实盘密钥分别配置 + 连通性测试）
- [ ] 认证模块（JWT 登录）
- [ ] 基础前端框架与布局（含环境标识 UI）

### 阶段二：核心交易功能（Week 3-4）

- [ ] 行情服务（WebSocket 订阅 + Redis 缓存）
- [ ] 策略引擎（定时周期触发 + 状态机）
- [ ] 订单服务（开仓 + TP/SL 条件单）
- [ ] 仓位管理（实时同步 + 盈亏计算）
- [ ] TP/SL 监控

### 阶段三：风控与监控（Week 5）

- [ ] 风控模块（建仓前检查 + 运行时熔断）
- [ ] 实时推送网关（Socket.io）
- [ ] 前端仪表盘（K线图 + 持仓表 + 盈亏曲线）
- [ ] 策略管理页面（CRUD + 启停）

### 阶段四：完善与测试（Week 6）

- [ ] 统计分析页面
- [ ] 设置页面（交易所配置 + 风控参数）
- [ ] Paper Trading 模式（模拟盘测试）
- [ ] 集成测试 + 异常场景测试
- [ ] 部署上线

### 里程碑

| 里程碑 | 时间 | 交付物 |
|--------|------|--------|
| M1 框架就绪 | Week 2 | 可运行空框架 + 交易所连通（Testnet 验证通过） |
| M2 核心交易闭环 | Week 4 | 模拟盘自动周期开仓 + TP/SL 精确平仓 Q |
| M3 完整控制台 | Week 5 | Web 界面 + 实时监控 + 模拟盘/实盘切换 |
| M4 上线 | Week 6 | 模拟盘充分验证 → 实盘 API 配置 → 生产部署 |

---

## 十二、风险评估

### 12.1 技术风险

| 风险 | 等级 | 应对 |
|------|------|------|
| 交易所 API 变更 | 中 | ccxt 抽象层隔离，锁定版本 |
| 行情延迟/断连 | 高 | 自动重连 + 状态同步 + 断连暂停开仓 |
| 重复下单 | 高 | 订单幂等性 + 数据库唯一约束 |
| 数据丢失 | 中 | PostgreSQL 持久化 + 定期备份 |

### 12.2 交易风险

| 风险 | 等级 | 应对 |
|------|------|------|
| 策略亏损 | 高 | 严格风控 + 模拟盘先行 + 最大亏损熔断 |
| 极端行情（插针） | 高 | 交易所原生条件单 + 合理 SL |
| 资金费率损耗 | 中 | 策略参数中考虑资金费率成本 |
| 流动性不足 | 低 | 限制交易对为主流币种 |

### 12.3 合规风险

> **⚠️ 合规提示**
>
> 数字货币交易在各国家和地区的法律地位不同。中国大陆明确禁止虚拟货币交易炒作活动。本项目仅作为技术研究与学习用途，实际部署和交易需确保符合当地法律法规。建议：
>
> - 优先使用模拟盘（Testnet）进行策略验证
> - 了解并遵守所在地区的法律法规
> - 不构成任何投资建议，交易风险自负

---

## 十三、后续演进方向

1. **多交易所接入** — OKX、Bybit 等扩展
2. **策略扩展** — 支持更多策略类型（网格、马丁、趋势跟踪）
3. **回测引擎** — 历史数据回测 + 参数优化
4. **通知系统** — Telegram / 飞书 / 邮件告警
5. **多用户支持** — SaaS 化，多账户管理
6. **移动端适配** — 响应式布局 / 小程序

---

## 附录：技术栈版本清单

| 组件 | 版本 |
|------|------|
| Node.js | 20 LTS |
| NestJS | 10.x |
| React | 18.x |
| Vite | 5.x |
| Prisma | 5.x |
| TypeScript | 5.x |
| PostgreSQL | 16 |
| Redis | 7.x |
| ccxt | 4.x |
| Docker | 24+ |
| Nginx | 1.25+ |

---

_本文档为 HermesX 项目立项技术方案，后续根据评审反馈迭代更新。_
